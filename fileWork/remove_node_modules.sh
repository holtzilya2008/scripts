#!/bin/bash

if [ -z "$1" ]; then
  echo "Usage: $0 <path>"
  exit 1
fi

starting_path="$1"

find "$starting_path" -type d -name 'node_modules' -exec rm -rf {} +

echo "All 'node_modules' directories have been removed from $starting_path and its subdirectories."
